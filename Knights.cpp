//
// Created by aach on 11.03.2026.
//

#include "Knights.h"

/* void Priest::Knights(int rounds) {
    int VSs[2] = {int& VSFire, int& VSPoison, int& VSIce};
    size_t n = sizeof(VSs) / sizeof(VSs[0]);
    if (rounds <= 3) {
        for (size_t i = 0; i < n; i++) {
            if (i != 1) {
                i
            }
        }
    }
} */